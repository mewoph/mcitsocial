<!DOCTYPE html>
<html>
<head>
  <title>MCIT Social</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <link href='http://fonts.googleapis.com/css?family=Lobster' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Roboto:300,400,700,900,500' rel='stylesheet' type='text/css'>
  <%= csrf_meta_tags %>
</head>
<body>

	<div class="navbar">
	  <div class="navbar-inner">
	    <a class="brand" href="#">mcit social</a>
	    <ul class="nav">
	      <li class=<%= is_active('/')%>><a href="#">MCITSocial</a></li>
	      <li class=<%= is_active('/users') %>><%= link_to "Users", users_path %></li>
	      <% if not current_user.nil? %>
	      	<li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
	      	<% else %>
			<li class=<%= is_active('/signup') %>> <%= link_to "Sign up", new_user_registration_path %></li>
	      <% end %>
	    </ul>
	    <ul class="nav navbar-right">
	    	<li class="sign-in-status">Signed in as User</li>
	    </ul>
	  </div>
	</div>
	<div class="row-fluid">
	    <div class="span10 offset1">
			<% if notice %>
			    <p class="alert alert-notice"><%= notice %></p>
			<% end %>
			<% if alert %>
		  	    <p class="alert alert-error"><%= alert %></p>
			<% end %>

			<%= yield %>

		</div>
	</div>
</body>
</html>
